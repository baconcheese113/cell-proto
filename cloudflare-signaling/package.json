{
  "name": "cellproto-signaling",
  "version": "1.0.0",
  "description": "Cloudflare Workers signaling server for Cell Proto multiplayer",
  "main": "src/worker.ts",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "build": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250829.0",
    "typescript": "^5.9.2",
    "wrangler": "^4.33.1"
  }
}
